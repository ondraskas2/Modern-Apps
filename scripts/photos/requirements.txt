# CLIP vision encoder download + validation (Xenova/mobileclip_s0).
huggingface_hub>=0.23
onnx>=1.16
onnxruntime>=1.19
numpy>=1.24

# EdgeFace export only (HuggingFace transformers mirror -> ONNX before INT8).
# Heavy; skip if you only run `prepare_models.py --clip`.
torch>=2.2
transformers>=4.44
timm>=1.0
safetensors>=0.4
